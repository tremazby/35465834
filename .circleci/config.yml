version: 2
jobs:
  Reactz:
    resource_class: large
    docker:
      - image: docker:18.06.3-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build react application Docker image
          no_output_timeout : 1h
          command: |
            docker run -e CI=true ulminase/hello-world:latest
      ###############################################################
  Expressz:
      resource_class: large
      docker:
        - image: docker:18.06.3-ce-git
      steps:
        - checkout
        - setup_remote_docker
        - run:
            name: Build Express application Docker image
            no_output_timeout : 1h
            command: |
              docker run -e CI=true ulminase/hello-world:latest
  React1z:
    resource_class: large
    docker:
      - image: docker:18.06.3-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build react application Docker image
          no_output_timeout : 1h
          command: |
            docker run -e CI=true ulminase/hello-world:latest
      
  Express1z:
      resource_class: large
      docker:
        - image: docker:18.06.3-ce-git
      steps:
        - checkout
        - setup_remote_docker
        - run:
            name: Build Express application Docker image
            no_output_timeout : 1h
            command: |
              docker run -e CI=true ulminase/hello-world:latest
  React2z:
    resource_class: large
    docker:
      - image: docker:18.06.3-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build react application Docker image
          no_output_timeout : 1h
          command: |
            docker run -e CI=true ulminase/hello-world:latest
      
  
        
        
workflows:
  version: 2
  build_test_feature:
    jobs:
      - Reactz:
          filters:
            branches:
              ignore:
                - main
                - master
      - Expressz:
          filters:
            branches:
              ignore:
                - main
                - master
      - React1z:
          filters:
            branches:
              ignore:
                - main
                - master
      - Express1z:
          filters:
            branches:
              ignore:
                - main
                - master
      - React2z:
          filters:
            branches:
              ignore:
                - main
                - master
      
